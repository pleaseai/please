# SonarQube Cloud project config
# https://docs.sonarsource.com/sonarqube-cloud/advanced-setup/analysis-parameters/
# projectKey and organization must match the values SonarCloud shows for this project.

sonar.projectKey=pleaseai_please
sonar.organization=pleaseai

# Sources and tests. This is a workspace, so the paths are per package - add the
# next package here when one is created.
sonar.sources=packages/core/src,packages/cli/src
sonar.tests=packages/core/test,packages/cli/test

# Coverage reports emitted by the test runner - the same files Codecov uploads
sonar.javascript.lcov.reportPaths=packages/core/coverage/lcov.info,packages/cli/coverage/lcov.info

# Exclude generated, vendored, and coverage output from analysis. Tests live outside
# sonar.sources, so no file is in both sets and none has to be excluded to keep the
# scanner from indexing it twice.
sonar.exclusions=**/dist/**,**/node_modules/**,**/coverage/**
sonar.coverage.exclusions=**/*.test.*,**/*.spec.*,**/*.config.*
